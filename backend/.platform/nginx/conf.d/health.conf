# Elastic Beanstalk's default nginx times out long requests at 60s. Claude
# API calls can take 45s+, so bump the proxy timeout to 120s. Keeps EB's
# health check on /health responsive while allowing slow AI routes.
proxy_read_timeout 120s;
proxy_send_timeout 120s;
